// Minimal per-section layout for /about/*
// NOTE: do NOT render <html>/<head>/<body> here.
// Root layout already does that.

export default function AboutSectionLayout({ children }) {
  return <>{children}</>;
}
